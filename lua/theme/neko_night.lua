-- theme
return {
  "neko-night/nvim",
  lazy = false,
  priority = 1000,
  opts = {},
  config = function()
    -- vim.cmd([[colorscheme nekonight-doom-one]])
    -- vim.cmd([[colorscheme nekonight-zenburn]])
  end,
}
