return {}
-- return { "nvim-neorg/neorg", opts = {} }
-- return { "https://github.com/lubyk/yaml" }

-- Packages are not limited to just Neovim plugins. You can install any luarocks package
-- Luarocks packages without a directory are never lazy-loaded, since it's just a library