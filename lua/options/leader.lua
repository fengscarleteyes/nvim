-- ============================================================
-- 前缀键 leader；必须在 lua/keymaps/*.lua 之前生效
-- （root init.lua 里 require("options") 在 require("keymaps") 之前，顺序已保证）
-- ============================================================

vim.g.mapleader = " "
vim.g.maplocalleader = "\\"
